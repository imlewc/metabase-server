{
  "name": "metabase-server",
  "version": "0.1.0",
  "description": "A Model Context Protocol server",
  "private": true,
  "type": "module",
  "bin": {
    "metabase-server": "./build/index.js"
  },
  "files": [
    "build"
  ],
  "scripts": {
    "build": "tsc && node -e \"require('fs').chmodSync('build/index.js', '755')\" && mkdir -p dist && cp build/index.js dist/index.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "inspector": "npx @modelcontextprotocol/inspector build/index.js",
    "config": "tsc && node build/config-generator.js",
    "config:simple": "tsc && node build/simple-config-generator.js",
    "config:quick": "tsc && node build/quick-config.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "@types/inquirer": "^8.2.12",
    "abort-controller": "^3.0.0",
    "axios": "^1.8.2",
    "inquirer": "^8.2.7"
  },
  "devDependencies": {
    "@types/axios": "^0.14.4",
    "@types/node": "^20.17.22",
    "chalk": "^5.6.2",
    "typescript": "^5.3.3"
  }
}
